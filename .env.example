# Database Configuration (Neon)
# Copy this file to .env and replace with your real Neon credentials
DATABASE_URL=postgresql://<username>:<password>@<neon-host>/<database>?sslmode=require&channel_binding=require

# SSL for managed databases like Neon
DB_SSL=true

# Server Configuration
PORT=3000
NODE_ENV=development

